<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="android:Theme.Holo.Light">
        <!-- Customize your theme here. -->
    </style>

    <style name="Theme.NoTitleBar" parent="android:Theme.NoTitleBar">
        <!-- Customize your theme here. -->
    </style>

    <style name="Theme.CustDialog" parent="android:Theme.Holo.Light.Dialog">
        <!-- Customize your theme here. -->
    </style>

    <style name="PreferencesTheme" parent="@android:style/Theme.Holo.Light">
        <item name="android:textSize">15sp</item>
        <item name="android:textColorSecondary">@color/secondary_text_color_selector</item>
        <item name="android:textColorPrimary">@color/primary_text_color_selector</item>
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:listPreferredItemHeight">36dp</item>
        <item name="android:dividerPadding">0dp</item>
    </style>

</resources>
